module examples.if_local_branch

// Statement-shaped branch that updates one mutable local on both paths.
fn main() -> Int32 {
    var value: Int32 = 10
    if value < 11 {
        value = value + 1
    } else {
        value = value + 2
    }
    return value
}
