[package]
name = "network-reactor-server-project"
version = "0.1.0"
edition = "2026"

[dependencies]
jadren-app = { path = "../../stdlib/app" }
jadren-network = { path = "../../stdlib/network" }

[targets]
library = true
